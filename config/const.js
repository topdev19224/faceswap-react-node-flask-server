const ROOT = `${__dirname.substr(0, __dirname.lastIndexOf("\\"))}`
const STOCK_PATH = "../back-end/store/images/stocks"
const IMAGE_PATH = "/store/images/"
const AI_SERVER_ENDPOINT = 'http://localhost:5000'

module.exports = {
  ROOT,
  STOCK_PATH,
  IMAGE_PATH,
  AI_SERVER_ENDPOINT
}